package broker.object;

/**
 * The parent interface for all application objects that are distributed and
 * managed by the Broker. The Interface has a semantically meaning only.
 */
public interface DistributedObject {

	
}
